import { ScrollUpBtnDirective } from './scroll-up-btn.directive';

describe('ScrollUpBtnDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollUpBtnDirective();
    expect(directive).toBeTruthy();
  });
});
